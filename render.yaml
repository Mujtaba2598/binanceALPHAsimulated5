services:
  - type: web
    name: halal-simple-bot
    runtime: node
    plan: free
    buildCommand: npm install && mkdir -p data/trades
    startCommand: node server.js
    healthCheckPath: /api/health
